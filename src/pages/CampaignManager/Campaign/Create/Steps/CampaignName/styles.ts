import { Section } from 'components/grid/wrappers/FlexWrapper';
import { white } from 'constants/styles';
import styled from 'styled-components';

export const CampaignNameWrapper = styled(Section)`
    height: 105px;
    padding: 16px 156px;
    background-color: ${white};
`;
