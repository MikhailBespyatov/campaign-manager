import { wrapperBorder, wrapperPadding } from 'pages/CampaignManager/Campaign/Details/constants';
import styled from 'styled-components';

export const Wrapper = styled.div`
    width: 100%;
    border: ${wrapperBorder};
    border-top: none;
    padding: ${wrapperPadding};
`;
