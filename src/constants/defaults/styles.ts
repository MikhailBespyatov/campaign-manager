import { primaryColor } from '../styles';

export const defaultFontFamily = 'Montserrat';
export const defaultFontStyle = 'normal';
export const defaultFontWeight = '600';
export const defaultFontSize = '14px';
export const defaultLineHeight = '16px';
export const defaultTextColor = primaryColor;
