export interface CardModal {
    visible: boolean;
    id: string;
}

export interface PopUpCampaignManager {
    visible: boolean;
    popUp: 'info' | 'discard';
}

export interface QexWidgetModal {
    visible: boolean;
}
