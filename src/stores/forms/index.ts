import { createCampaignForm } from 'stores/forms/createCampaignForm';
import { channelForm } from 'stores/forms/channelForm';
import { productForm } from 'stores/forms/productForm';

export const forms = { createCampaignForm, channelForm, productForm };
