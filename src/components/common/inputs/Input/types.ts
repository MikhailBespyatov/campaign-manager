import { Background, Color, MinSizes, Sizes, TextAlignment, TextProperties } from 'types';

export interface InputProps extends Sizes, MinSizes, TextProperties, Color, Background, TextAlignment {}
