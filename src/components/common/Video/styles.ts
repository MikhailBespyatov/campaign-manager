import styled from 'styled-components';

export const StyledVideo = styled.video`
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    &:focus {
        outline: none;
    }
`;
